services:
  - type: web
    name: nextrouter
    runtime: node
    region: oregon
    plan: starter
    buildCommand: npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000